[project]
name = "democritus"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "together>=1.3.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
